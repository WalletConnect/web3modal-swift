import Foundation

enum Radius {
    /// Default 4 points
    static var masterRadius: CGFloat = 4
    
    /// Default 4 points
    static var xxxxxs: CGFloat = masterRadius
    /// Default 6 points
    static var xxxxs: CGFloat = masterRadius * 1.5
    /// Default 8 points
    static var xxxs: CGFloat = masterRadius * 2
    /// Default 12 points
    static var xxs: CGFloat = masterRadius * 3
    /// Default 16 points
    static var xs: CGFloat = masterRadius * 4
    /// Default 20 points
    static var s: CGFloat = masterRadius * 5
    /// Default 28 points
    static var m: CGFloat = masterRadius * 7
    /// Default 36 points
    static var l: CGFloat = masterRadius * 9
    /// Default 80 points
    static var xxxl: CGFloat = masterRadius * 20
}
